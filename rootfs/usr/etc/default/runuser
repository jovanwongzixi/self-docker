# /etc/default/runuser is an override of /etc/login.defs for runuser.
# See /etc/login.defs and runuser(1) for more.
#
# List of supported variables:
# ALWAYS_SET_PATH, ENV_PATH, ENV_ROOTPATH, ENV_SUPATH, FAIL_DELAY
#
